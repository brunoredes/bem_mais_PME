export class PlanPriceModel {
  planId: string;
  planPriceId: string;
  name: string;
  timeCoverage: number;
  installments: number;
  installmentValue: string;
  amount: string;
  selected: boolean;
}
